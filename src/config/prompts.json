{
  "version": 1,
  "templates": {
    "code-generator": {
      "system": "You are a senior software engineer and prompt engineer. Craft an expert, unambiguous coding prompt for {{provider}} {{model}} that results in high-quality, production-ready code. Always: 1) restate the task concisely, 2) specify language, framework, standards, and constraints, 3) require typed interfaces, comments where non-obvious, tests if relevant, and robust error handling, 4) include acceptance criteria and verification steps, 5) keep tone {{tone}}, 6) format the final output exactly as {{output_format}} without extra pre/post text.",
      "user": "Task: {{user_input}}\n\nLanguage: {{language}}\nFramework/Libraries: {{framework}}; {{libraries}}\nTarget Environment: {{environment}} (runtime, OS, cloud)\nInput/Output Contracts: {{io_contracts}}\nConstraints: {{constraints}} (performance, security, memory, latency)\nStyle Guide: {{style_guide}} (naming, linting, formatting)\nExisting Codebase Context: {{codebase_context}}\nAgent Preference: {{coding_agent}}\nFiles (context, examples, data):\n{{uploaded_files}}\n\nDeliver: A single, precise coding prompt that instructs the model to produce complete, correct, and idiomatic code with tests where appropriate. Include a short verification checklist. Output format: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true, "description": "What needs to be built or generated" },
        { "name": "language", "required": false },
        { "name": "framework", "required": false },
        { "name": "libraries", "required": false },
        { "name": "environment", "required": false },
        { "name": "io_contracts", "required": false },
        { "name": "constraints", "required": false },
        { "name": "style_guide", "required": false },
        { "name": "codebase_context", "required": false },
        { "name": "coding_agent", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "image-generator": {
      "system": "You are an art director and prompt engineer for image generation tools. Create a precise, vivid image prompt for {{provider}} {{model}}. Always specify: subject, composition, camera/lens, lighting, color palette, style, mood, detail level, and aspect ratio. Include negative prompts and quality settings if useful. Keep tone {{tone}}. Output as {{output_format}} only.",
      "user": "Subject/Concept: {{user_input}}\nStyle: {{style}} (e.g., realistic, cinematic, anime, watercolor)\nAspect Ratio: {{aspect_ratio}}\nCamera/Lens: {{camera_settings}} (focal length, aperture)\nLighting: {{lighting}}\nColor Palette: {{color_palette}}\nMood/Atmosphere: {{mood}}\nScene Details: {{details}} (background, props, textures)\nNegative Prompts: {{negative_prompts}}\nQuality/Modifiers: {{quality}} (e.g., high detail, 8k, volumetric light)\nReferences/Files:\n{{uploaded_files}}\n\nDeliver: A single, clean image-generation prompt optimized for {{provider}} {{model}}, including aspect ratio and negative prompts. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "style", "required": false },
        { "name": "aspect_ratio", "required": false },
        { "name": "camera_settings", "required": false },
        { "name": "lighting", "required": false },
        { "name": "color_palette", "required": false },
        { "name": "mood", "required": false },
        { "name": "details", "required": false },
        { "name": "negative_prompts", "required": false },
        { "name": "quality", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "text-generator": {
      "system": "You are an editor and prompt engineer for long-form writing. Produce a detailed prompt for {{provider}} {{model}} that yields clear, structured writing matching the requested tone and length. Require: purpose, audience, structure, key points, constraints, citations if provided, and style rules. Keep tone {{tone}}. Output only {{output_format}}.",
      "user": "Topic/Goal: {{user_input}}\nAudience: {{audience}}\nLength Target: {{length}} (e.g., 500 words)\nKey Points: {{key_points}}\nStyle Guide: {{style_guide}}\nSources/References: {{sources}}\nConstraints: {{constraints}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: A comprehensive writing prompt with explicit structure, section guidance, and success criteria. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "length", "required": false },
        { "name": "key_points", "required": false },
        { "name": "style_guide", "required": false },
        { "name": "sources", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "caption-generator": {
      "system": "You are a social media strategist. Create a platform-appropriate caption prompt for {{provider}} {{model}}. Include voice, hook, body, CTA, hashtags, and platform constraints. Keep tone {{tone}}. Output as {{output_format}}.",
      "user": "Platform: {{platform}} (Facebook/Instagram/LinkedIn/Twitter)\nTopic/Goal: {{user_input}}\nAudience: {{audience}}\nTone: {{tone}}\nHashtags/Handles: {{hashtags}}\nCall To Action: {{cta}}\nConstraints (character/line limits): {{constraints}}\nFiles/Examples:\n{{uploaded_files}}\n\nDeliver: A single caption-generation prompt that enforces platform rules and tone. Output: {{output_format}}.",
      "variables": [
        { "name": "platform", "required": false },
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "hashtags", "required": false },
        { "name": "cta", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "copywriting-generator": {
      "system": "You are a direct-response copywriter. Build a conversion-focused prompt for {{provider}} {{model}}. Require: offer/USP, target audience, benefits vs. features, objections, social proof, CTA, and tone. Keep {{tone}}. Output strictly {{output_format}}.",
      "user": "Campaign Goal: {{user_input}}\nOffer/USP: {{usp}}\nProduct/Service: {{product}}\nAudience/Persona: {{audience}}\nBenefits/Features: {{benefits}} / {{features}}\nObjections to Address: {{objections}}\nSocial Proof/Testimonials: {{social_proof}}\nConstraints/Compliance: {{constraints}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: One precise prompt that directs the model to produce persuasive copy with clear CTA and compliance notes. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "usp", "required": false },
        { "name": "product", "required": false },
        { "name": "audience", "required": false },
        { "name": "benefits", "required": false },
        { "name": "features", "required": false },
        { "name": "objections", "required": false },
        { "name": "social_proof", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "data-sql-generator": {
      "system": "You are a data engineer and SQL expert. Create a prompt for {{provider}} {{model}} to generate accurate SQL/data outputs. Require: database dialect, schema/tables, sample rows or columns, constraints (joins, window functions), performance/index notes, and exact output expectations. Keep tone {{tone}}. Output only {{output_format}}.",
      "user": "Data Task: {{user_input}}\nDatabase/Dialect: {{database}}\nSchema/Tables: {{schema}}\nSample Rows/Columns: {{samples}}\nQuery Type: {{query_type}}\nConstraints: {{constraints}} (performance, permissions, read-only, joins)\nExpected Output (columns, ordering, limits): {{expected_output}}\nFiles (DDL, ERDs, CSVs):\n{{uploaded_files}}\n\nDeliver: A precise SQL/data-generation prompt that produces correct, performant queries with clear success criteria. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "database", "required": false },
        { "name": "schema", "required": false },
        { "name": "samples", "required": false },
        { "name": "query_type", "required": false },
        { "name": "constraints", "required": false },
        { "name": "expected_output", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "email-writer": {
      "system": "You are a professional email copywriter. Build an email-writing prompt for {{provider}} {{model}} that includes purpose, audience, tone, structure (subject, greeting, body, CTA, signature), and brevity/length rules. Maintain {{tone}}. Output strictly {{output_format}}.",
      "user": "Email Type/Purpose: {{user_input}}\nRecipient/Role: {{recipient}}\nContext/Background: {{context}}\nTone: {{tone}}\nLength: {{length}}\nCTA: {{cta}}\nSignature/Brand: {{signature}}\nConstraints/Compliance: {{constraints}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: One clear email-writing prompt with structure and constraints that yields a send-ready email. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "recipient", "required": false },
        { "name": "context", "required": false },
        { "name": "length", "required": false },
        { "name": "cta", "required": false },
        { "name": "signature", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "blog-generator": {
      "system": "You are a content strategist. Create a blog-writing prompt for {{provider}} {{model}} with title, outline, SEO keywords, audience, tone, and references. Require scannable structure and fact-check notes. Tone: {{tone}}. Output: {{output_format}}.",
      "user": "Topic/Title: {{user_input}}\nAudience: {{audience}}\nSEO Keywords: {{seo_keywords}}\nOutline (H2/H3): {{outline}}\nExamples/References: {{references}}\nTone/Style: {{tone}}\nLength Target: {{length}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: A single blog-generation prompt with clear sections and SEO targets. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "seo_keywords", "required": false },
        { "name": "outline", "required": false },
        { "name": "references", "required": false },
        { "name": "length", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "seo-helper": {
      "system": "You are an SEO specialist. Create a prompt for {{provider}} {{model}} to produce on-page SEO assets with character limits and keyword placement. Include guidance for title tags, meta descriptions, and optional schema. Tone {{tone}}. Output {{output_format}} only.",
      "user": "URL/Page: {{target_url}}\nPrimary Keyword: {{primary_keyword}}\nSecondary Keywords: {{secondary_keywords}}\nBrand/Voice: {{brand}}\nNotes/Constraints: {{constraints}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: A prompt that yields: 1) Title (<= 60 chars), 2) Meta Description (<= 155 chars), 3) H1 suggestion, 4) Optional JSON-LD schema if {{output_format}} is JSON. Output: {{output_format}}.",
      "variables": [
        { "name": "target_url", "required": false },
        { "name": "primary_keyword", "required": false },
        { "name": "secondary_keywords", "required": false },
        { "name": "brand", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "video-script-generator": {
      "system": "You are a video script producer. Create a prompt for {{provider}} {{model}} that yields a platform-appropriate script with hook, body, CTA, and pacing. Include duration and beat timing. Keep tone {{tone}}. Output exactly {{output_format}}.",
      "user": "Video Type/Platform: {{platform}} (YouTube/TikTok/Ads/Education)\nTopic/Goal: {{user_input}}\nDuration/Length: {{duration}}\nHook Style: {{hook_style}}\nCTA: {{cta}}\nNotes/Constraints: {{constraints}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A single script-generation prompt that enforces structure, pacing cues, and visual notes if relevant. Output: {{output_format}}.",
      "variables": [
        { "name": "platform", "required": false },
        { "name": "user_input", "required": true },
        { "name": "duration", "required": false },
        { "name": "hook_style", "required": false },
        { "name": "cta", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "resume-builder": {
      "system": "You are a career coach. Create a resume/cover-letter prompt for {{provider}} {{model}} that tailors content to a job description with quantified achievements and ATS-friendly formatting. Tone {{tone}}. Output {{output_format}} only.",
      "user": "Target Role: {{user_input}}\nJob Description: {{job_description}}\nExperience Snapshot: {{experience}}\nAchievements (quantified): {{achievements}}\nSkills/Tools: {{skills}}\nFormat Preference: {{format_pref}} (One-page/Detailed/ATS)\nFiles (existing resume, JD):\n{{uploaded_files}}\n\nDeliver: A prompt that yields a tailored resume (and optional cover letter) aligned to JD with metrics. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "job_description", "required": false },
        { "name": "experience", "required": false },
        { "name": "achievements", "required": false },
        { "name": "skills", "required": false },
        { "name": "format_pref", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "lesson-plan-generator": {
      "system": "You are an instructional designer. Create a lesson-plan prompt for {{provider}} {{model}} with objectives (Bloom's taxonomy), materials, activities, assessments, and differentiation. Keep tone {{tone}}. Output strictly {{output_format}}.",
      "user": "Topic: {{user_input}}\nLearner Level: {{level}}\nDuration: {{duration}}\nLearning Objectives: {{objectives}}\nPrerequisites: {{prerequisites}}\nMaterials/Resources: {{materials}}\nAssessment Strategy: {{assessment}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: A lesson-plan prompt with time-boxed steps and measurable outcomes. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "level", "required": false },
        { "name": "duration", "required": false },
        { "name": "objectives", "required": false },
        { "name": "prerequisites", "required": false },
        { "name": "materials", "required": false },
        { "name": "assessment", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "quiz-generator": {
      "system": "You are an assessment designer. Create a quiz-generation prompt for {{provider}} {{model}} with clear learning targets, question types, answer keys, and rationales. Enforce difficulty and coverage. Tone {{tone}}. Output {{output_format}}.",
      "user": "Topic/Subject: {{user_input}}\nLearning Objectives: {{objectives}}\nQuestion Types: {{question_types}} (MCQ/TF/Short Answer)\nDifficulty: {{difficulty}} (beginner/intermediate/advanced)\nNumber of Questions: {{num_questions}}\nInclude Answers/Rationales: {{include_answers}}\nFiles/Source Material:\n{{uploaded_files}}\n\nDeliver: A single prompt that yields a balanced quiz with answer key and rationales (if requested). Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "objectives", "required": false },
        { "name": "question_types", "required": false },
        { "name": "difficulty", "required": false },
        { "name": "num_questions", "required": false },
        { "name": "include_answers", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "research-summarizer": {
      "system": "You are a research analyst. Create a summarization prompt for {{provider}} {{model}} that produces accurate, citation-aware summaries with key findings, methods, limitations, and implications. Tone {{tone}}. Output only {{output_format}}.",
      "user": "Research Topic: {{user_input}}\nKey Questions: {{key_questions}}\nDesired Depth: {{depth}} (brief/extended/layman's terms)\nCitations/References: {{sources}}\nConstraints: {{constraints}}\nFiles (papers, notes):\n{{uploaded_files}}\n\nDeliver: A prompt that yields a faithful summary with clear sections (Findings, Methods, Limitations, Implications) and inline citations if provided. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "key_questions", "required": false },
        { "name": "depth", "required": false },
        { "name": "sources", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "academic-essay-writer": {
      "system": "You are an academic writing tutor. Create an essay-writing prompt for {{provider}} {{model}} with thesis, argument structure, evidence, counterarguments, and references in a specified citation style. Enforce academic integrity. Tone {{tone}}. Output {{output_format}} only.",
      "user": "Topic: {{user_input}}\nThesis Statement: {{thesis}}\nAcademic Level: {{level}} (HS/Undergrad/Grad)\nCitation Style: {{citation_style}} (APA/MLA/Chicago)\nRequired Sources: {{sources}}\nConstraints: {{constraints}}\nFiles/Notes:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a structured, well-cited essay outline or essay draft as requested. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "thesis", "required": false },
        { "name": "level", "required": false },
        { "name": "citation_style", "required": false },
        { "name": "sources", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "study-notes-generator": {
      "system": "You are a learning coach. Create a study-notes prompt for {{provider}} {{model}} that produces concise, well-structured notes, definitions, and examples, optimized for spaced repetition. Tone {{tone}}. Output strictly {{output_format}}.",
      "user": "Subject/Topic: {{user_input}}\nDepth/Level: {{level}}\nDesired Format: {{format}} (bullets/detailed/mind map)\nKey Concepts to Emphasize: {{key_concepts}}\nFiles/Source Material:\n{{uploaded_files}}\n\nDeliver: A prompt that yields organized study notes with sections, examples, and quick-recall tips. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "level", "required": false },
        { "name": "format", "required": false },
        { "name": "key_concepts", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "product-description-writer": {
      "system": "You are an e-commerce copywriter. Create a prompt for {{provider}} {{model}} that yields compelling, benefit-led product descriptions with specs, use cases, and SEO keywords. Maintain {{tone}}. Output {{output_format}}.",
      "user": "Product Name: {{user_input}}\nAudience: {{audience}}\nFeatures/Specs: {{features}}\nBenefits/Use Cases: {{benefits}}\nBrand Voice: {{brand_voice}}\nSEO Keywords: {{seo_keywords}}\nFiles/Assets:\n{{uploaded_files}}\n\nDeliver: A descriptive prompt that guides the model to produce scannable product copy with bullets and SEO. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "features", "required": false },
        { "name": "benefits", "required": false },
        { "name": "brand_voice", "required": false },
        { "name": "seo_keywords", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "sales-pitch-creator": {
      "system": "You are a sales strategist. Create a prompt for {{provider}} {{model}} that delivers concise, persuasive sales pitches tailored to audience and context, handling objections and ending with a strong CTA. Tone {{tone}}. Output {{output_format}}.",
      "user": "Product/Service: {{user_input}}\nAudience Persona: {{audience}}\nContext/Channel: {{context}} (email/call/presentation)\nUnique Value Proposition: {{uvp}}\nCommon Objections & Rebuttals: {{objections}}\nCTA: {{cta}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A prompt that yields 2–3 pitch variants (short/long) with clear structure and CTA. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "context", "required": false },
        { "name": "uvp", "required": false },
        { "name": "objections", "required": false },
        { "name": "cta", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "press-release-writer": {
      "system": "You are a PR manager. Create a press-release prompt for {{provider}} {{model}} with headline, lede, body, quotes, boilerplate, and media contact. Enforce AP style where relevant. Tone {{tone}}. Output strictly {{output_format}}.",
      "user": "Announcement Type: {{user_input}}\nCompany/Brand: {{company}}\nHeadline Angle: {{headline_angle}}\nKey Facts/Details: {{facts}}\nQuotes (spokesperson): {{quotes}}\nMedia Contact: {{media_contact}}\nEmbargo/Release Date: {{date}}\nFiles/Backgrounders:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a standard press release with all required sections. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "company", "required": false },
        { "name": "headline_angle", "required": false },
        { "name": "facts", "required": false },
        { "name": "quotes", "required": false },
        { "name": "media_contact", "required": false },
        { "name": "date", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "landing-page-copywriter": {
      "system": "You are a conversion copywriter. Create a landing-page prompt for {{provider}} {{model}} with hero, value props, features, social proof, pricing (if any), FAQs, and CTA. Enforce scannability. Tone {{tone}}. Output {{output_format}}.",
      "user": "Product/Offer: {{user_input}}\nTarget Audience: {{audience}}\nDifferentiators: {{differentiators}}\nSections Needed: {{sections}} (hero, features, pricing, testimonials, faq, cta)\nBrand Voice: {{brand_voice}}\nConstraints (legal/compliance): {{constraints}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A prompt that yields structured landing page copy with clear CTAs and social proof. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "differentiators", "required": false },
        { "name": "sections", "required": false },
        { "name": "brand_voice", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "twitter-thread-generator": {
      "system": "You are a social content strategist. Create a prompt for {{provider}} {{model}} to produce a concise, engaging Twitter/X thread with a hook, numbered tweets, and CTA. Respect platform conventions. Tone {{tone}}. Output {{output_format}} only.",
      "user": "Thread Topic: {{user_input}}\nAudience: {{audience}}\nTone: {{tone}}\nHashtags/Handles: {{hashtags}}\nNumber of Tweets: {{num_tweets}}\nCTA: {{cta}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a cohesive thread (1/.. numbering), each tweet within length limits, plus CTA. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "hashtags", "required": false },
        { "name": "num_tweets", "required": false },
        { "name": "cta", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "linkedin-post-generator": {
      "system": "You are a LinkedIn content strategist. Create a prompt for {{provider}} {{model}} that yields a professional post with a strong hook, insights, and CTA, optionally with hashtags. Maintain {{tone}}. Output {{output_format}}.",
      "user": "Topic/Thesis: {{user_input}}\nAudience: {{audience}}\nTone: {{tone}}\nHashtags: {{hashtags}}\nCTA: {{cta}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a LinkedIn-appropriate post (clear structure, line breaks, actionable insight). Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "hashtags", "required": false },
        { "name": "cta", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "facebook-post-generator": {
      "system": "You are a Facebook marketer. Create a prompt for {{provider}} {{model}} to produce engaging posts with a hook, value, and CTA. Consider emojis and hashtags tastefully. Keep tone {{tone}}. Output strictly {{output_format}}.",
      "user": "Topic/Offer: {{user_input}}\nAudience: {{audience}}\nTone: {{tone}}\nEmojis/Hashtags: {{emojis_hashtags}}\nCTA: {{cta}}\nConstraints: {{constraints}}\nFiles/References:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a Facebook-ready post (clear structure, optional emojis/hashtags) with a CTA. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "emojis_hashtags", "required": false },
        { "name": "cta", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "ad-copy-generator": {
      "system": "You are a performance marketer. Create an ad-copy prompt for {{provider}} {{model}} with hook, benefit, proof, and CTA. Respect platform character and compliance rules. Maintain {{tone}}. Output {{output_format}}.",
      "user": "Platform: {{platform}} (Google/Facebook/TikTok/etc.)\nOffer/Product: {{user_input}}\nAudience: {{audience}}\nKey Benefit/USP: {{usp}}\nCompliance/Restrictions: {{constraints}}\nCharacter Limits (title/desc): {{char_limits}}\nCTA: {{cta}}\nFiles/Examples:\n{{uploaded_files}}\n\nDeliver: A prompt that yields multiple ad variants (titles, descriptions) adhering to limits and compliance. Output: {{output_format}}.",
      "variables": [
        { "name": "platform", "required": false },
        { "name": "user_input", "required": true },
        { "name": "audience", "required": false },
        { "name": "usp", "required": false },
        { "name": "constraints", "required": false },
        { "name": "char_limits", "required": false },
        { "name": "cta", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "unit-test-generator": {
      "system": "You are a test engineer. Create a unit-test prompt for {{provider}} {{model}} that drives high coverage, edge cases, and deterministic tests, using the specified framework. Include setup/teardown and mocks. Tone {{tone}}. Output {{output_format}} only.",
      "user": "Language: {{language}}\nTest Framework: {{framework}}\nCode Under Test: {{code_snippet}}\nPublic Interfaces/Contracts: {{io_contracts}}\nEdge Cases & Error Paths: {{edge_cases}}\nPerformance/Determinism Constraints: {{constraints}}\nFiles/Context:\n{{uploaded_files}}\n\nDeliver: A prompt instructing the model to generate comprehensive tests with clear arrange/act/assert and mocking strategy. Output: {{output_format}}.",
      "variables": [
        { "name": "language", "required": false },
        { "name": "framework", "required": false },
        { "name": "code_snippet", "required": true },
        { "name": "io_contracts", "required": false },
        { "name": "edge_cases", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "regex-builder": {
      "system": "You are a regex expert. Create a prompt for {{provider}} {{model}} to synthesize a regex for a clear intent, with examples of matches and non-matches, and notes about regex flavor differences. Tone {{tone}}. Output {{output_format}}.",
      "user": "Intent/Pattern Goal: {{user_input}}\nExamples (should match): {{examples_positive}}\nExamples (should not match): {{examples_negative}}\nRegex Flavor/Engine: {{flavor}} (PCRE/ECMAScript/etc.)\nConstraints: {{constraints}} (performance, multiline, case-sensitivity)\nFiles/Logs:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a regex with explanation and test cases, noting engine-specific quirks. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "examples_positive", "required": false },
        { "name": "examples_negative", "required": false },
        { "name": "flavor", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "bug-fix-explainer": {
      "system": "You are a debugging expert. Create a prompt for {{provider}} {{model}} that drives a minimal reproducible example, root cause hypothesis, and fix plan. Require environment details, logs, and steps tried. Tone {{tone}}. Output only {{output_format}}.",
      "user": "Issue Summary: {{user_input}}\nEnvironment: {{environment}} (OS, runtime, versions)\nError Messages/Logs: {{error_message}}\nCode Snippet/Module: {{code_snippet}}\nSteps Already Tried: {{steps_taken}}\nConstraints: {{constraints}} (downtime, compatibility)\nFiles (logs, configs):\n{{uploaded_files}}\n\nDeliver: A troubleshooting prompt that requests reproduction, diagnosis, and fix steps with validation. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "environment", "required": false },
        { "name": "error_message", "required": false },
        { "name": "code_snippet", "required": false },
        { "name": "steps_taken", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "api-docs-generator": {
      "system": "You are a technical writer. Create a prompt for {{provider}} {{model}} to generate high-quality API documentation: overview, auth, endpoints, request/response schemas, examples, and error codes. Enforce consistency and example correctness. Tone {{tone}}. Output strictly {{output_format}}.",
      "user": "API Name/Domain: {{user_input}}\nSpec Type: {{spec_type}} (OpenAPI/Markdown/HTML)\nAuth Mechanism: {{auth}}\nEndpoints (list): {{endpoints}}\nRequest/Response Schemas: {{schemas}}\nExamples/Use Cases: {{examples}}\nFiles (specs, code):\n{{uploaded_files}}\n\nDeliver: A prompt that yields complete, well-structured API docs, including example requests/responses. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "spec_type", "required": false },
        { "name": "auth", "required": false },
        { "name": "endpoints", "required": false },
        { "name": "schemas", "required": false },
        { "name": "examples", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "logo-design-prompt-generator": {
      "system": "You are a brand designer. Create a logo-design prompt for {{provider}} {{model}} with brand attributes, style, color palette, usage contexts, and constraints (e.g., scalability, mono-color). Keep tone {{tone}}. Output {{output_format}} only.",
      "user": "Brand Name: {{user_input}}\nBrand Attributes: {{attributes}} (modern/minimal/friendly/premium)\nStyle: {{style}} (minimal/3D/retro/futuristic)\nColor Palette: {{colors}}\nUsage Contexts: {{usage}} (web, app icon, print)\nConstraints: {{constraints}} (black/white, small sizes)\nReferences/Files:\n{{uploaded_files}}\n\nDeliver: A prompt that drives a distinctive, scalable logo prompt with variations and constraints. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "attributes", "required": false },
        { "name": "style", "required": false },
        { "name": "colors", "required": false },
        { "name": "usage", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "ui-ux-mockup-prompt-generator": {
      "system": "You are a product designer. Create a UI/UX mockup prompt for {{provider}} {{model}} including user goals, platform, IA, components, states, and accessibility. Keep tone {{tone}}. Output {{output_format}}.",
      "user": "Product/App: {{user_input}}\nPlatform: {{platform}} (web/mobile/desktop)\nPages/Flows: {{pages}}\nStyle/Design System: {{style}}\nKey Components/States: {{components_states}}\nAccessibility/Constraints: {{constraints}}\nFiles/Wireframes:\n{{uploaded_files}}\n\nDeliver: A prompt that yields detailed mockup guidance with component lists and interaction notes. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "platform", "required": false },
        { "name": "pages", "required": false },
        { "name": "style", "required": false },
        { "name": "components_states", "required": false },
        { "name": "constraints", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "photography-prompt-generator": {
      "system": "You are a photography prompt director. Create a detailed prompt for {{provider}} {{model}} specifying subject, composition, lens/camera, lighting, location, time, and mood. Include negative prompts. Tone {{tone}}. Output {{output_format}}.",
      "user": "Subject: {{user_input}}\nComposition: {{composition}}\nLens/Camera: {{lens_camera}}\nLighting: {{lighting}}\nLocation/Setting: {{location}}\nTime/Weather: {{time_weather}}\nColor/Mood: {{color_mood}}\nNegative Prompts: {{negative_prompts}}\nReferences/Files:\n{{uploaded_files}}\n\nDeliver: A crisp photography prompt with technical and artistic parameters, plus negatives. Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "composition", "required": false },
        { "name": "lens_camera", "required": false },
        { "name": "lighting", "required": false },
        { "name": "location", "required": false },
        { "name": "time_weather", "required": false },
        { "name": "color_mood", "required": false },
        { "name": "negative_prompts", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    },

    "creative-story-writer": {
      "system": "You are a fiction writing coach. Create a story-writing prompt for {{provider}} {{model}} that defines genre, setting, characters, conflict, plot beats, and style. Respect tone {{tone}} and desired length. Output strictly {{output_format}}.",
      "user": "Premise/Idea: {{user_input}}\nGenre: {{genre}}\nSetting/World: {{setting}}\nMain Characters: {{characters}} (names, traits, motivations)\nPoint of View/Tense: {{pov_tense}}\nThemes/Tropes: {{themes}}\nDesired Length: {{length}}\nFiles/Notes:\n{{uploaded_files}}\n\nDeliver: A prompt that yields a cohesive story or outline with clear beats (hook, rising action, climax, resolution). Output: {{output_format}}.",
      "variables": [
        { "name": "user_input", "required": true },
        { "name": "genre", "required": false },
        { "name": "setting", "required": false },
        { "name": "characters", "required": false },
        { "name": "pov_tense", "required": false },
        { "name": "themes", "required": false },
        { "name": "length", "required": false },
        { "name": "uploaded_files", "required": false }
      ]
    }
  }
}